import { Content } from '@components/Search';
import { Header } from '@components/Common';

const Search = () => {
  return (
    <>
      <Header text="검색" />
      <Content />
    </>
  );
};

export default Search;
